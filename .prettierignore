.next
node_modules
pnpm-lock.yaml
next-env.d.ts

# Generated by `supabase gen types` and never hand-edited (CLAUDE.md). The
# generator emits double quotes, so Prettier would rewrite the file on every
# regeneration — making `format:check` fail after every migration until someone
# remembers a manual pass. Ignored for the same reason next-env.d.ts is.
src/types/database.ts
coverage
supabase/migrations

# Markdown is hand-maintained prose — CLAUDE.md, AGENTS.md and the context/ docs are
# the project's source of truth, and reflowing their tables and paragraphs creates
# large diffs that bury the actual edit. Format them by hand.
*.md
